server:
  port: 8080
  servlet:
    context-path: /SpringBoot_ActiveMQ
    
spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    in-memory: true
    pool:
      enabled: false